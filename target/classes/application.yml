spring:
  application:
    name: GameHub
  datasource:
    password: postgres
    url: jdbc:postgresql://localhost:5432/gamehub
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true